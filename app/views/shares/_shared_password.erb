<div class="py-2 grid grid-cols-3">
  <div><%= user.email %></div>
  <div class="text-center"><%= user_role %></div>

  <div class="text-right">
    <%= button_to "Remove",
                  password_share_path(password, user),
                  method: :delete,
                  data: { turbo_confirm: "Are you sure?" } if shareable

    %>
  </div>
</div>